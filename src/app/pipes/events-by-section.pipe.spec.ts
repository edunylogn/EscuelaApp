import { EventsBySectionPipe } from './events-by-section.pipe';

describe('EventsBySectionPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsBySectionPipe();
    expect(pipe).toBeTruthy();
  });
});
